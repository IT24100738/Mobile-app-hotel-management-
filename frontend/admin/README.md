WMT Admin Dashboard (scaffold)

Quick start:

1. cd frontend/admin
2. npm install
3. npm start

This is a minimal scaffold. Implement CRUD pages under `src/pages` and integrate with the backend API (`/api/...`).
